/**
 * we're using regular expression here to partially assert 
 */

export const appurls = {
        screenName: {
                booking: /.*booking/,
                RollNos: /.*gtyh/,
                Marks: /.*xxxxuiasd&/,
        },
        commonUrls: {
                documents: /.*documents/,
                userSettingsUrl: /.*settings/,
        }
}